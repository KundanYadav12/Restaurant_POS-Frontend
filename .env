# =========================================================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# =========================================================================
# This file is automatically used by Vite when building for production (npm run build).
# You DO NOT need to comment this out when testing locally.
# Vite will automatically switch to using '.env.development' when you run 'npm run dev'.
VITE_API_URL=https://fastorder.duckdns.org/api
