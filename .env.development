# =========================================================================
# LOCAL DEVELOPMENT ENVIRONMENT CONFIGURATION
# =========================================================================
# This file is automatically loaded by Vite only when running locally (npm run dev).
# All requests are proxied locally to http://localhost:5004.
# You DO NOT need to comment this out when deploying. Vite automatically ignores
# this file during the production build process.
VITE_API_URL=/api
